{
  "compilerOptions": {
    "outDir": "dist", // where to put the TS files
    "target": "ES2015", // which level of JS support to target
    "moduleResolution": "Node10"
  },
  "include": ["src"] // which files to compile
  // "exclude": ["node_modules"]
}
